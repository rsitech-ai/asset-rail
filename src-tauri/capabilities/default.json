{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "AssetRail main planner window with no built-in core or plugin permissions.",
  "windows": ["main"],
  "permissions": []
}
